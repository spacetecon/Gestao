# Configuração do Servidor
NODE_ENV=development
PORT=3001

# Banco de Dados PostgreSQL
# Formato: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
DATABASE_URL="postgresql://postgres:[YOUR_PASSWORD]@db.dqfvnzuubxptedsrvpzn.supabase.co:5432/postgres"

# JWT - Gere uma chave segura aleatória
# Pode usar: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=sua_chave_super_secreta_aqui_troque_isso

# Validade do token (em dias)
JWT_EXPIRES_IN=7d

# CORS - Domínios permitidos (separados por vírgula)
CORS_ORIGIN=http://localhost:5173,http://localhost:3000

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logs
LOG_LEVEL=info
